{
  "name": "@hayabusa/api-schema",
  "version": "0.1.0",
  "repository": "https://github.com/dino3616/hayabusa.git",
  "license": "MIT",
  "private": true,
  "scripts": {
    "generate": "bun ./generate",
    "fmt": "biome format .",
    "fmt:fix": "biome format --write",
    "lint": "biome lint .",
    "lint:fix": "biome lint --apply",
    "test": "bun test",
    "gql:publish": "dotenv -- rover graph publish hayabusa@current --schema ./generated/schema.gql"
  },
  "dependencies": {
    "gql-tag": "1.0.1",
    "graphql": "16.9.0"
  },
  "devDependencies": {
    "@apollo/rover": "0.26.2"
  }
}
